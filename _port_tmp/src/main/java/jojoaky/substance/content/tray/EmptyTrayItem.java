package jojoaky.substance.content.tray;

import net.minecraft.world.item.BlockItem;

public class EmptyTrayItem extends BlockItem {
    public EmptyTrayItem(EmptyTrayBlock block, Properties properties) {
        super(block, properties);
    }
}
